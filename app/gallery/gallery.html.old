<div class="my-gallery">
    
    <div class="container">
        <div class="row ">
        <div class="gallery col-lg-12 col-md-12 col-sm-12 col-xs-12">
            <h1 class="gallery-title">Let's start - choose a course</h1>
        </div>
 <!--      <div align="center">
            <button class="btn btn-default filter-button">All</button>
            <button class="btn btn-default filter-button">Your Courses</button>
            <button class="btn btn-default filter-button">Other Courses</button>
        </div> -->
        <br/>
            <div class="gallery_product col-lg-4 col-md-4 col-sm-4 col-xs-6 {{course.id}}" ng-repeat="course in courses | filter:filterByCourseId()">
               <div class="well" >
                <img src="{{course.courseUrl}}" class="img-responsive gallery-item" ng-click="toTimeline()">
                <figcaption>{{course.name}}  {{course.desc}}</figcaption>
                </div>             
            </div>
        </div>
    </div>
</div>
